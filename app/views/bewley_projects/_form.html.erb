<%= form_with(model: bewley_project) do |form| %>
  <% if bewley_project.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bewley_project.errors.count, "error") %> prohibited this bewley_project from being saved:</h2>

      <ul>
        <% bewley_project.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
